import { Dispatch } from "react"
import { EdcucationActionTypes, EducationAction } from "../../types/educationType"

export const fetchEducation = () => {
    return (dispatch: Dispatch<EducationAction>) => {
        // dispatch({type: EdcucationActionTypes.FETCH_EDUCATION, payload:  })
    }
}